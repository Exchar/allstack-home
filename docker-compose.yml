version: '3.8'

services:
  allstack-home:
    image: exchar/allstack-home:latest
    container_name: allstack-home
    ports:
      - "8888:8888"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
    restart: unless-stopped
    # 如果需要外部访问，请取消注释以下行
    # networks:
    #   - web

# networks:
#   web:
#     external: true